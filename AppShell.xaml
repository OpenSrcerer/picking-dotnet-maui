<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    FlyoutBehavior="Disabled"
    x:Class="Project_CS412.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:views="clr-namespace:Project_CS412.Views"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ShellContent ContentTemplate="{DataTemplate views:MasterTabulatedView}" Route="MainPage" />
    <ShellContent ContentTemplate="{DataTemplate views:EasterEggView}" Route="EasterEggPage" />
    <ShellContent ContentTemplate="{DataTemplate views:DetailTabulatedView}" Route="DetailPage" />
    <ShellContent ContentTemplate="{DataTemplate views:ItemOrderLineView}" Route="ItemOrderLinePage" />
    <ShellContent ContentTemplate="{DataTemplate views:ItemOrderView}" Route="ItemOrderPage" />
    <ShellContent ContentTemplate="{DataTemplate views:ItemPkgView}" Route="ItemPkgPage" />
    <ShellContent ContentTemplate="{DataTemplate views:StorePosView}" Route="StorePosPage" />
    <ShellContent ContentTemplate="{DataTemplate views:StoreView}" Route="StorePage" />
    <ShellContent ContentTemplate="{DataTemplate views:VatClassView}" Route="VatClassPage" />

</Shell>
